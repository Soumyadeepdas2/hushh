# SPA fallback for Cloudflare Pages: serve index.html for client-side routes.
# Place at the root of the deployed site (or in /public to be copied as-is).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
